// src/app/lib/constants.js

// The base URL for your app, typically set via environment variable
export const BASE_URL = process.env.NEXT_PUBLIC_API_URL;

// If you want to hardcode a fallback, you can do:
// export const BASE_URL = process.env.NEXT_PUBLIC_API_URL || "https://yourdomain.com";